<%= content_for :title, @post && @post[:title] %>

<%= vue_component("application", {posts: @posts || nil, post: @post || nil, diary: @diary},
  {
    prerender: request.path,
    state: {
        hasLogin: current_user.present?,
        csrfToken: form_authenticity_token
    }
  })
%>
