<%= vue_component("application", {posts: @posts || nil, post: @post || nil},
  {
    prerender: request.path,
    state: {
        hasLogin: current_user.present?,
        csrfToken: form_authenticity_token
    }
  })
%>
